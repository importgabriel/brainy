{
  "name": "brainy",
  "private": true,
  "type": "module",
  "version": "1.0.0",
  "description": "Cortex — Universal AI context memory",
  "scripts": {
    "postinstall": "cd switchvampire && npm install",
    "build": "cd switchvampire && npm run build",
    "start": "cd switchvampire && npm start"
  }
}
